# Read zram files.
allow system_app sysfs_zram:dir r_dir_perms;
allow system_app sysfs_zram:file r_file_perms;
allow system_app {
    storaged
    system_suspend
}:binder { call };

# Allow systemapps to check incremental progress 
get_prop(system_app, incremental_prop)